// export const logger = (state) => (next) => (action) => {
//   console.log("prv: ", state.getState());
//   const result = next(action);
//   console.log("Next: ", state.getState());
//   return result;
// };
